# 来源提取修正前的尝试

`source-extraction-1/` 保留最初输入、段落与目录映射、来源说明、冻结记录及初次导入的运行文件。初次正文含两处网页工具附加的域名尾标；发现时已有 14 个导入块，尚无逐块分析和最终报告。修正详情见 [source-correction.json](../source-correction.json)，归档后的文件哈希见 [aborted-attempt-hashes.json](../aborted-attempt-hashes.json)。

日志中的绝对路径记录当时位置。旧运行目录后来整体移入这里，当前 `../run/` 使用修正后的输入；不要将旧命令中的路径当作可直接重放的当前位置。旧日志和材料没有随迁移改写。

本机保留归档数据库，仓库通用规则排除 SQLite 文件。可提交的原始命令输出、输入材料及初次导入结果用于核查此次基础设施恢复；它不是第二份完成报告，也不参与语义比较。
